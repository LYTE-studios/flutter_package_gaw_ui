import 'package:flutter/material.dart';

class GawTheme {
  static const Color mainTint = Color.fromRGBO(78, 171, 233, 1);

  static const Color secondaryTint = Color.fromRGBO(53, 115, 183, 1);

  static const Color background = Color.fromRGBO(240, 240, 240, 1);

  static const Color darkBackground = Color.fromRGBO(83, 84, 92, 1);

  static const Color pickerBackground = Color.fromRGBO(229, 229, 229, 1.0);

  static const Color clearBackground = Color.fromRGBO(250, 250, 250, 1);

  static const Color unselectedBackground = Color.fromRGBO(217, 217, 217, 1);

  static const Color border = Color.fromRGBO(189, 189, 189, 1);

  static const Color toolBarItem = Color.fromRGBO(155, 155, 155, 1);

  static const Color text = Color.fromRGBO(51, 54, 63, 1);

  static const Color specialText = Color.fromRGBO(66, 84, 102, 1);

  static const Color specialBackground = Color.fromRGBO(246, 246, 251, 1);

  static const Color interText = Color.fromRGBO(18, 18, 18, 1);

  static const Color unselectedText = Color.fromRGBO(142, 146, 149, 1);

  static const Color unselectedMainTint = Color.fromRGBO(161, 213, 255, 0.6);

  static const Color mainTintText = Color.fromRGBO(248, 248, 248, 1);

  static const Color clearText = Colors.white;

  static const Color mainTintUnselectedText =
      Color.fromRGBO(248, 248, 248, 0.7);

  static const Color shadow = Color.fromRGBO(142, 146, 149, 0.1);

  static const Color darkShadow = Color.fromRGBO(142, 146, 149, 0.2);

  static const Color blur = Color.fromRGBO(0, 0, 0, 0.35);

  static const Color error = Color.fromRGBO(209, 61, 28, 1);

  static const Color success = Color.fromRGBO(0, 168, 48, 1);

  static const Color statsText = Color.fromRGBO(144, 147, 157, 1);
}
