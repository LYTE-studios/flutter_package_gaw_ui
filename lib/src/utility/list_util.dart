import 'package:flutter/material.dart';

class ListUtil {
  static const double sColumn = 80;
  static const double mColumn = 120;
  static const double lColumn = 160;

  static Widget makeHeader(Map<String, double> labels) {
    return Row(
      children: [],
    );
  }

  static Widget makeRow(Map<Widget, double> widgets) {
    return Row(children: [],);
  }
}
